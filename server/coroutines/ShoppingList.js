const echokit = require('../../EchoKit')

const storage = echokit.storageBlock('shopping-list')
const list = storage.get('list') || []

const coroutine = (envelope) => {
    const 
}

module.exports = coroutine